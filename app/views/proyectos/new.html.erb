<% if current_user %>
	<div>
	  <h1>Proyectos</h1>
		<div>
		  <%= render 'proyectos/form' %>
		</div>

		<div class="down">
		  <p><%= link_to 'Back', proyectos_path %></p>
		</div>
	</div>
<% end %>

<% if session[:session] %>
  <p><%= session[:errors] %></p>
  <% session[:errors].clear %>
<% end %>