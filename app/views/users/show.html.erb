<p id="notice"><%= notice %></p>

<p>
	<strong>Name:</strong>
	<%= @user.name %>
	<strong>Email:</strong>
	<%= @user.email %>
</p>

<%= link_to 'Sign in', sign_in_form_path %> 
<%= link_to 'Back', users_path %>